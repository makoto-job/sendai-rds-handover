# 明日（4/16）Surfaceから続きをやる手順書

## 前提
- 会社のSurface から 自宅のMac Mini に接続して作業する
- Mac Mini は 9:30 に自動で起きる設定済み

---

## ステップ1: SurfaceでPowerShellを開く

1. Windowsキーを押す
2. 「powershell」と入力
3. 「Windows PowerShell」をクリック

---

## ステップ2: Mac Miniに接続する

PowerShellに以下を入力してEnter:

```
ssh makotomini@100.118.60.78
```

- パスワードを聞かれたら、Mac Miniのログインパスワードを入力
- 「繋がらない」場合 → Mac Miniがまだスリープ中。10:00以降に再試行

---

## ステップ3: Claude Codeを起動する

SSH接続できたら、以下を入力してEnter:

```
cd /Volumes/mac_2TB/lifelog-repo/02_work/voice_handover_app && claude
```

---

## ステップ4: Claudeに続きをお願いする

Claude Codeが起動したら、以下をコピペして送信:

```
引き継ぎ帳アプリの開発の続き。前回のセッションでやったこと:
- Firebase Hosting にデプロイ済み（https://hikitsugi-app-2026.web.app）
- 匿名認証＋パスコード方式でログインできるようになった
- 担当者名がログイン名から自動追加されるようにした
- セキュリティルール（Firestore）を設定済み

まだやっていないこと（設計v2から）:
- XSSエスケープの統一（印刷HTML）
- JSONインポートのバリデーション
- Service Workerの改善
- 監査ログの実装
- iframe印刷への切替
- 管理者画面（メンバー管理）

設計v2_全問題解決版.md を読んで、続きから進めてください。
```

---

## うまくいかないときは

### SSHで繋がらない場合
- 10:00前だとMac Miniが寝ている → 10:00以降に再試行
- Tailscaleが切れている → Surfaceのタスクバーで Tailscale アイコンを確認、接続されているか確認

### Claude Codeが起動しない場合
- `claude` とだけ入力して試す
- エラーが出たらそのメッセージを読んで対処

### 前回の会話を見たい場合
- Claude Code 起動後に `/resume` と入力すると前回のセッションを再開できる

---

## 今日やったことのまとめ

1. Firebase プロジェクト作成（hikitsugi-app-2026）
2. Firestore セキュリティルール設定
3. 匿名認証＋パスコード方式でログイン機能を実装
4. Firebase Hosting にデプロイ
5. スマホでログイン成功を確認
6. 担当者名のプルダウン自動追加
7. Mac Mini のスリープ制御スクリプトのバグ修正
