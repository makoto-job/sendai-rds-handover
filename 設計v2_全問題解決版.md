# 仙台RDS 引継帳 — 設計v2（全問題解決版）

**作成日**: 2026-04-10
**前提**: 設計レビュー_2026-04-10.md で洗い出した全問題を解決する

---

## 全体構成図

```
┌─────────────────────────────────────────────────────────────────┐
│                        Firebase プロジェクト                      │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────────────┐ │
│  │   Hosting    │  │     Auth     │  │      Firestore        │ │
│  │  (PWA配信)   │  │  (電話番号   │  │  セキュリティルール    │ │
│  │  HTTPS強制   │  │   ログイン)  │  │  でガッチリ守る       │ │
│  └──────┬───────┘  └──────┬───────┘  └──────────┬─────────────┘ │
│         │                 │                      │               │
└─────────┼─────────────────┼──────────────────────┼───────────────┘
          │ HTTPS           │ SMS認証              │ 認証済みのみ
          ▼                 ▼                      ▼
┌─────────────────────────────────────────────────────────────────┐
│  [現場スマホ A]     [現場スマホ B]     [事務所 Surface]          │
│   モバイル回線       ホテルWiFi        ポケットWiFi             │
│                                                                  │
│   PWAアプリ          PWAアプリ          PWAアプリ               │
│   ├ Firebase Auth    ├ Firebase Auth    ├ Firebase Auth          │
│   ├ localStorage     ├ localStorage     ├ localStorage           │
│   └ オフライン対応   └ オフライン対応   └ オフライン対応         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 解決策マッピング

| # | 問題 | 解決策 | 対応箇所 |
|---|---|---|---|
| 1 | Firebase認証なし | 電話番号認証を導入 | auth.js（新規）+ app.js |
| 2 | API Key平文保存 | Firebase Hosting使用でconfigをコードに埋込（公開前提の正しい使い方） | firebase-sync.js |
| 3 | セキュリティルール不明 | 認証＋メンバー限定ルールを定義 | firestore.rules（新規） |
| 4 | XSS | 全出力をエスケープ関数経由に統一 | app.js |
| 5 | JSONインポート無検証 | スキーマバリデーション追加 | app.js |
| 6 | Firebase SDK SRIなし | integrity属性追加 | index.html |
| 7 | CSPなし | meta tagでCSP設定 | index.html |
| 8 | PII保護なし | Firestoreルールでアクセス制限＋監査ログ | firestore.rules |
| 9 | SW検証なし | バージョンハッシュ＋ネットワーク優先フォールバック | sw.js |
| 10 | window.open印刷 | iframe方式に変更（ブロッカー回避） | app.js |
| 11 | 信頼できない回線 | HTTPS強制（Firebase Hosting）＋CSP | Hosting設定 |
| 12 | 管理外端末 | 認証＋セッション管理＋ログアウト機能 | auth.js |
| 13 | 監査ログなし | 誰が・いつ・何を変更したか記録 | firebase-sync.js |
| 14 | ホスティング先未定 | Firebase Hosting | firebase.json（新規） |
| 15 | 複数会社のアクセス管理 | プロジェクトメンバー制＋ロール（管理者/作業者） | Firestoreデータモデル |

---

## 1. 認証：電話番号ログイン

### なぜ電話番号？
- 現場の作業員は全員スマホを持っている
- メールアドレスより確実（仕事用メール持ってない人もいる）
- パスワードを覚える必要がない（SMS認証コード）
- 1回ログインすればセッションが続く

### ログインの流れ

```
1. アプリを開く
2. 初回のみ → ログイン画面が表示される
3. 電話番号を入力（例: 090-xxxx-xxxx）
4. SMSで6桁のコードが届く
5. コードを入力 → ログイン完了
6. 次回以降はログイン不要（セッション保持）
```

### 管理者の操作

```
1. 管理者がFirebase Consoleまたはアプリの管理画面で
   「この電話番号の人を参加させる」と登録
2. 登録されていない電話番号ではログインできても
   データにアクセスできない（セキュリティルールで弾く）
```

### 新規ファイル: auth.js

```javascript
/* 認証モジュール */
const Auth = {
  _user: null,
  _onAuthChange: null,

  async init(onAuthChange) {
    this._onAuthChange = onAuthChange;

    firebase.auth().onAuthStateChanged(user => {
      this._user = user;
      onAuthChange(user);
    });
  },

  // 電話番号でSMS送信
  async sendCode(phoneNumber, recaptchaContainer) {
    const verifier = new firebase.auth.RecaptchaVerifier(recaptchaContainer, {
      size: 'invisible'
    });
    const result = await firebase.auth().signInWithPhoneNumber(phoneNumber, verifier);
    return result; // confirmationResult
  },

  // SMS認証コードを確認
  async verifyCode(confirmationResult, code) {
    const result = await confirmationResult.confirm(code);
    return result.user;
  },

  // ログアウト
  async signOut() {
    await firebase.auth().signOut();
  },

  // 現在のユーザー
  getUser() { return this._user; },
  getUid() { return this._user?.uid || null; },
  getPhone() { return this._user?.phoneNumber || null; }
};
```

---

## 2. Firestore セキュリティルール

### 新規ファイル: firestore.rules

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ===== プロジェクト =====
    match /projects/{projectId} {

      // --- メンバー ---
      // 読み取り: ログイン済みなら誰でも（自分がメンバーか確認するため）
      // 書き込み: 管理者のみ
      match /members/{memberId} {
        allow read: if request.auth != null;
        allow create, update, delete: if request.auth != null
          && isMember(projectId, request.auth.uid)
          && getMemberRole(projectId, request.auth.uid) == 'admin';
      }

      // --- 引継帳シート ---
      // メンバーだけが読み書きできる
      match /sheets/{sheetId} {
        allow read: if request.auth != null
          && isMember(projectId, request.auth.uid);
        allow create, update: if request.auth != null
          && isMember(projectId, request.auth.uid)
          && request.resource.data.updatedBy == request.auth.uid;
        allow delete: if request.auth != null
          && isMember(projectId, request.auth.uid)
          && getMemberRole(projectId, request.auth.uid) == 'admin';
      }

      // --- 監査ログ ---
      // 作成のみ（書き換え・削除は誰にもできない）
      match /audit/{logId} {
        allow create: if request.auth != null
          && isMember(projectId, request.auth.uid);
        allow read: if request.auth != null
          && isMember(projectId, request.auth.uid);
        allow update, delete: if false;
      }
    }

    // ===== ヘルパー関数 =====
    function isMember(projectId, uid) {
      return exists(/databases/$(database)/documents/projects/$(projectId)/members/$(uid));
    }
    function getMemberRole(projectId, uid) {
      return get(/databases/$(database)/documents/projects/$(projectId)/members/$(uid)).data.role;
    }
  }
}
```

### ルールの意味（わかりやすく）

```
倉庫（Firestore）
├── projects/sendai_rds_2026/
│   ├── members/          ← 「名簿」登録した人しか入れない
│   │   ├── {ユーザーA}   → role: "admin"（管理者）
│   │   ├── {ユーザーB}   → role: "worker"（作業者）
│   │   └── {ユーザーC}   → role: "worker"
│   │
│   ├── sheets/           ← 引継帳データ（メンバーだけ読み書き）
│   │   ├── 2026-04-10_day
│   │   └── 2026-04-10_night
│   │
│   └── audit/            ← 変更ログ（追記のみ、消せない）
│       ├── {自動ID}  → "ユーザーBが04-10昼勤を保存 14:30"
│       └── {自動ID}  → "ユーザーAが04-10夜勤を保存 22:15"
```

- **ログインしてない** → 何も見えない、何もできない
- **ログインしたけどメンバーじゃない** → 何も見えない、何もできない
- **メンバー（作業者）** → シートの読み書きができる。削除はできない
- **メンバー（管理者）** → 全部できる。メンバーの追加・削除もできる
- **監査ログ** → 誰にも消せない。改ざん不可能

---

## 3. Firestore データモデル

### シートデータ（変更点）

```javascript
// 保存時に追加されるフィールド
{
  date: "2026-04-10",
  shift: "day",
  weather: "晴れ",
  supervisor: "田中",
  // ...既存フィールドそのまま...

  // ★追加: 誰が最後に更新したか
  updatedBy: "firebase-uid-xxxxx",     // ユーザーID（自動）
  updatedByName: "佐藤",              // 表示名
  updatedByPhone: "090-xxxx-xxxx",    // 電話番号
  updatedAt: "2026-04-10T14:30:00Z",  // タイムスタンプ

  // ★追加: 作成者情報
  createdBy: "firebase-uid-xxxxx",
  createdAt: "2026-04-10T08:00:00Z"
}
```

### 監査ログ（新規）

```javascript
// 保存のたびに自動追記（firebase-sync.js内で実装）
{
  action: "update",                    // create / update / delete
  sheetId: "2026-04-10_day",
  userId: "firebase-uid-xxxxx",
  userName: "佐藤",
  timestamp: firebase.firestore.FieldValue.serverTimestamp(),
  // 変更されたフィールド名のリスト（データ量を抑えるため値は含めない）
  changedFields: ["weather", "reactors.RX-01A.entries"]
}
```

---

## 4. コードレベルの修正

### 4-1. XSS対策（app.js）

印刷HTML生成の全箇所で `esc()` を適用する。

```javascript
// 修正前（危険）
html += `<span>${e.catalyst}</span>`;
meta.push(`${e.fcCount}/${e.fcTotal||'?'}FC`);

// 修正後（安全）
html += `<span>${this.esc(e.catalyst)}</span>`;
meta.push(`${this.esc(e.fcCount)}/${this.esc(e.fcTotal)||'?'}FC`);
```

対象箇所:
- `buildEntryLines()` 内の全テンプレートリテラル
- `exportAllPDF()` 内の `buildLines()` 
- `renderEntries()` 内の meta 出力

### 4-2. JSONインポートのバリデーション（app.js）

```javascript
// Store.importAll() にバリデーション追加
importAll(json) {
  const data = JSON.parse(json);

  // バージョンチェック
  if (data.type !== 'sendai_rds') throw new Error('このアプリのデータではありません');

  // シートデータの構造検証
  if (data.sheets) {
    Object.entries(data.sheets).forEach(([k, v]) => {
      // キー形式チェック: YYYY-MM-DD_day|night
      if (!/^\d{4}-\d{2}-\d{2}_(day|night)$/.test(k)) {
        throw new Error(`不正なキー: ${k}`);
      }
      // 必須フィールドチェック
      if (v && typeof v !== 'object') throw new Error(`不正なデータ: ${k}`);
      // localStorage の hiki_ プレフィックス以外のキーは書かない
    });
  }
  // ...以降は既存処理
}
```

### 4-3. CSP メタタグ（index.html）

```html
<meta http-equiv="Content-Security-Policy" content="
  default-src 'self';
  script-src 'self' https://www.gstatic.com;
  style-src 'self' 'unsafe-inline';
  connect-src 'self' https://*.googleapis.com https://*.firebaseio.com wss://*.firebaseio.com;
  img-src 'self' data:;
  font-src 'self';
">
```

意味:
- `default-src 'self'` → 基本的に自分のサーバーからだけ読み込む
- `script-src` → JSは自サーバーとFirebase CDNだけ
- `connect-src` → 通信先はFirebase関連だけ
- 知らないサーバーからの読み込みを全部ブロック

### 4-4. Firebase SDK に SRI 追加（index.html）

```html
<script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-app-compat.js"
  integrity="sha384-（ハッシュ値）"
  crossorigin="anonymous"></script>
<script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-firestore-compat.js"
  integrity="sha384-（ハッシュ値）"
  crossorigin="anonymous"></script>
<!-- ★追加: Auth SDK -->
<script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-auth-compat.js"
  integrity="sha384-（ハッシュ値）"
  crossorigin="anonymous"></script>
```

※ ハッシュ値は実装時に `shasum` で取得する

### 4-5. Service Worker 改善（sw.js）

```javascript
const CACHE_NAME = 'vha-cache-v4';  // バージョンアップ

// ★変更: ネットワーク優先 → キャッシュフォールバック（アプリ本体）
self.addEventListener('fetch', (event) => {
  // Firebase API通信はキャッシュしない
  if (event.request.url.includes('googleapis.com') ||
      event.request.url.includes('firebaseio.com')) {
    event.respondWith(fetch(event.request));
    return;
  }

  // アプリ本体: ネットワーク優先、失敗時にキャッシュ
  event.respondWith(
    fetch(event.request)
      .then(response => {
        const clone = response.clone();
        caches.open(CACHE_NAME).then(cache => cache.put(event.request, clone));
        return response;
      })
      .catch(() => caches.match(event.request))
  );
});
```

### 4-6. 印刷方式の変更（app.js）

```javascript
// 修正前: window.open（ブロッカーで動かないことがある）
const w = window.open('', '_blank');
w.document.write(printHtml);

// 修正後: 非表示iframeを使う
printA4() {
  let iframe = document.getElementById('print-frame');
  if (!iframe) {
    iframe = document.createElement('iframe');
    iframe.id = 'print-frame';
    iframe.style.cssText = 'position:fixed;width:0;height:0;border:none;';
    document.body.appendChild(iframe);
  }
  const doc = iframe.contentDocument;
  doc.open();
  doc.write(printHtml);
  doc.close();
  setTimeout(() => iframe.contentWindow.print(), 400);
}
```

---

## 5. ファイル構成（v2）

```
voice_handover_app/
├── index.html              ← CSP追加、SRI追加、Auth SDK追加、ログイン画面追加
├── app.js                  ← XSS修正、バリデーション追加、iframe印刷
├── auth.js                 ★新規: 電話番号認証モジュール
├── firebase-sync.js        ← 認証連携、監査ログ追加
├── voice.js                （変更なし）
├── sendai-data.js          （変更なし）
├── style.css               ← ログイン画面のスタイル追加
├── sw.js                   ← ネットワーク優先に変更
├── manifest.json           （変更なし）
├── icons/
│   ├── icon-192.svg
│   └── icon-512.svg
├── firebase.json           ★新規: Hosting設定
├── firestore.rules         ★新規: セキュリティルール
├── .firebaserc             ★新規: Firebaseプロジェクト紐付け
├── 設計レビュー_2026-04-10.md
└── 設計v2_全問題解決版.md   ← このファイル
```

---

## 6. Firebase Hosting 設定

### firebase.json（新規）

```json
{
  "hosting": {
    "public": ".",
    "ignore": [
      "firebase.json",
      "firestore.rules",
      ".firebaserc",
      "*.md",
      ".git/**"
    ],
    "headers": [
      {
        "source": "**",
        "headers": [
          { "key": "X-Content-Type-Options", "value": "nosniff" },
          { "key": "X-Frame-Options", "value": "DENY" },
          { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
        ]
      }
    ]
  },
  "firestore": {
    "rules": "firestore.rules"
  }
}
```

### デプロイ手順

```bash
# 1. Firebase CLIインストール（初回のみ）
npm install -g firebase-tools

# 2. ログイン
firebase login

# 3. プロジェクト初期化（初回のみ）
cd /Volumes/mac_2TB/lifelog-repo/02_work/voice_handover_app
firebase init  # Hosting + Firestore を選択

# 4. デプロイ（更新のたびに実行）
firebase deploy

# → https://your-project-id.web.app でアクセス可能になる
```

---

## 7. 実装順序

```
Phase 1: 土台（まずFirebase環境を整える）
  ├── 1-1. Firebase Console でプロジェクト作成
  ├── 1-2. 電話番号認証を有効化
  ├── 1-3. Firestore セキュリティルールをデプロイ
  └── 1-4. Firebase Hosting でデプロイ（既存コードをそのまま）

Phase 2: 認証（ログイン機能を追加）
  ├── 2-1. auth.js 作成
  ├── 2-2. index.html にログイン画面を追加
  ├── 2-3. app.js で認証状態に応じた画面切替
  └── 2-4. firebase-sync.js に認証連携を追加

Phase 3: セキュリティ修正（コードの穴を塞ぐ）
  ├── 3-1. XSSエスケープの統一
  ├── 3-2. JSONインポートのバリデーション
  ├── 3-3. CSP metaタグ追加
  ├── 3-4. SRI追加
  └── 3-5. sw.js 改善

Phase 4: 監査と改善
  ├── 4-1. 監査ログの実装
  ├── 4-2. iframe印刷への切替
  └── 4-3. 管理者画面（メンバー管理）
```

---

## 補足: Firebase 無料枠で足りるか？

| リソース | 無料枠 | このアプリの想定使用量 |
|---|---|---|
| Hosting | 10GB/月 | アプリ本体100KB以下 → 余裕 |
| Firestore 読取 | 50,000回/日 | 30人×50回 = 1,500回/日 → 余裕 |
| Firestore 書込 | 20,000回/日 | 30人×20回 = 600回/日 → 余裕 |
| 電話認証SMS | 月10,000件 | 月30人×1回 = 30件 → 余裕 |
| ストレージ | 1GB | テキストのみ → 余裕 |

**結論: 無料枠（Sparkプラン）で収まる。課金は不要。**
