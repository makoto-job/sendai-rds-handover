# 仙台RDS 引継帳 — 全体設計レビュー

**レビュー日**: 2026-04-10
**対象**: voice_handover_app/（全9ファイル）

---

## アプリの概要

ENEOS仙台製油所の触媒交換工事で使う**日次引継帳のPWA**。
現場作業者が昼勤/夜勤ごとに2基のリアクター（RX-01A/02A）の作業実績を記録し、A4横の印刷帳票として出力する。

**運用**: Surfaceで遠隔操作する人が最新データ（正）を持つ。

---

## リモートアクセス構成

```
[会社 Surface] ──Tailscale VPN──→ [自宅 Mac Mini (100.118.60.78)]
                    SSH接続            ├── /Volumes/mac_2TB/lifelog-repo/
                                       └── 02_work/voice_handover_app/ ← 全ファイルここ
```

| 項目 | 状態 |
|---|---|
| SSH（リモートログイン） | 有効 |
| Tailscale 接続 | 有効（100.118.60.78） |
| スリープ制御 | 平日 9:30-17:30 のみ起動（15分ごと自動チェック） |

**セキュリティ評価**: Tailscale は WireGuard ベースの暗号化VPNで、ポートを外部に公開しない。SSH もTailscaleネットワーク内でしか到達できないため、この経路は安全。

> ※ これは開発時の構成。本番運用では使わない。

---

## 本番運用イメージ

```
[各社社員のスマホ] ──モバイル回線/ホテルWiFi/ポケットWiFi──→ Firebase
                                                                  ↕ 同期
[各社社員のスマホ] ──モバイル回線/ホテルWiFi/ポケットWiFi──→ Firebase
```

- **端末**: 各協力会社の社員がそれぞれの個人スマホを使用
- **回線**: モバイル回線 / ホテルWiFi / 事務所ポケットWiFi（いずれも管理外ネットワーク）
- **自宅Mac Miniは関係ない** — アプリはPWAとしてホスティングされ、Firebaseで同期する

### 本番で重要になるセキュリティポイント

| リスク | 説明 |
|---|---|
| **回線が信頼できない** | ホテルWiFi等は盗聴の可能性あり。Firebase通信はHTTPSなので通信経路自体は暗号化されるが、Firebase認証がないと「誰でもアクセス可能」は変わらない |
| **端末が管理外** | 各社社員の個人スマホ＝会社が管理できない端末。端末紛失時にlocalStorageのデータが漏洩するリスク |
| **複数会社の社員が同じデータにアクセス** | 誰が何を書き換えたかの記録（監査ログ）がない |
| **PWAのホスティング先** | 現時点で未定。GitHub Pages / Firebase Hosting / 社内サーバー等の選択が必要 |

---

## ファイル構成

| ファイル | 行数 | 役割 |
|---|---|---|
| `index.html` | 343行 | UI全体（1ページ完結） |
| `app.js` | 1125行 | アプリロジック全部（Store、UI制御、印刷、インポート/エクスポート） |
| `firebase-sync.js` | 177行 | Firestore同期＋オフライン永続化 |
| `voice.js` | 76行 | Web Speech API（日本語音声入力） |
| `sendai-data.js` | 188行 | 工事マスタデータ（作業項目、触媒名、リアクター定義） |
| `style.css` | 355行 | 全スタイル（ダーク対応） |
| `sw.js` | 44行 | Service Worker（キャッシュ優先） |
| `manifest.json` | 25行 | PWA定義 |
| `icons/` | 2 SVG | アイコン |

---

## データフロー

```
[現場iPhone/iPad] ──入力──→ localStorage
                              ↓ autoSave()
                        Firebase Firestore ←──→ [Surface（遠隔・正データ）]
                              ↓
                         リアルタイム同期
                              ↓
                       [別端末に反映]
```

- **一次保存**: localStorage（キー `hiki_{日付}_{day|night}`）
- **同期**: Firebase Firestore（オプション、設定画面からAPI Key/Project IDを入力）
- **オフライン**: Service Worker + Firestore永続化で圏外でも動作、復帰時に自動同期
- **出力**: A4横の帳票を `window.open` + `print()` で生成

---

## 機能一覧

1. 日付・シフト選択（昼勤/夜勤）
2. リアクタータブ（RX-01A / RX-02A）
3. 作業実績の記録（時間帯・触媒名・FC数・レベル・備考）
4. 作業項目チェックリスト（6カテゴリ、チェック→自動で実績作成）
5. 抜出/充填実績（時間・FC・レベル・温度・ドライアイス）
6. 客先立会い記録（設備検査・製油・GM承認・自衛防）
7. WBGT測定（3回分）
8. 引継ぎ事項（音声入力対応テキスト）
9. A4帳票印刷（紙の実績表と同一レイアウト再現）
10. 全日分PDF一括生成
11. JSON バックアップ/インポート
12. Firebase リアルタイム同期
13. ダークモード/担当者管理/日付一覧

---

## セキュリティ評価

### 重大（すぐに対応すべき）

#### 1. Firebase認証なし — 誰でもデータにアクセス可能
- `firebase-sync.js` で認証（`firebase.auth()`）を一切使っていない
- API Keyだけで Firestore に直接読み書きしている
- API Keyを知っている人なら誰でも全データを読み書き・削除できる
- Surface端末が正データだが、第三者が上書きできる状態

#### 2. Firebase API Key が localStorage に平文保存
- 場所: `firebase-sync.js:16-19`
- ブラウザのDevToolsで誰でも見える
- 端末を触れる全員がAPI Keyを取得可能

#### 3. Firestore セキュリティルールの設定が不明
- コード上にルール定義がない
- `allow read, write: if true;` のままなら世界中から操作可能
- → 詳細は別途「セキュリティルール解説」参照

### 中程度

#### 4. XSS（クロスサイトスクリプティング）リスク
- `app.js:757` の `buildEntryLines` で `meta.join(' ')` 等をエスケープなしでHTML挿入
- `esc()` は一部で使われているが印刷HTML生成では不完全
- 触媒名や備考に `<script>` を含む値が入ると printA4() 内で実行される

#### 5. JSONインポートにバリデーションなし
- 場所: `app.js:1015-1031`
- `Store.importAll()` は渡されたJSONを無検証で localStorage に書き込む
- 悪意あるJSONファイルで任意のlocalStorageキーを上書き可能

#### 6. Firebase SDK を CDN から SRI なしで読み込み
- 場所: `index.html:335-336`
- CDN侵害時にスクリプト改ざんを検知できない
- `integrity` 属性の追加が推奨

#### 7. Content Security Policy (CSP) なし
- `<meta>` タグにもHTTPヘッダーにもCSPがない
- インラインスクリプトや外部スクリプトの制限がゼロ

### 低〜注意レベル

#### 8. 個人情報（PII）の保護
- 担当者名（`vha_staff`）が localStorage + Firestore に平文で保存
- 客先立会者名も同様

#### 9. Service Worker のキャッシュ検証なし
- 場所: `sw.js:39-41`
- バージョン管理は `CACHE_NAME` の手動変更のみに依存

#### 10. `window.open` による印刷
- ポップアップブロッカーで動作しない可能性あり

---

## 推奨対策の優先順位

| 優先度 | 対策 | 工数 |
|---|---|---|
| **最優先** | Firebase Anonymous Auth または Email Auth を導入 + Firestore セキュリティルール修正 | 中 |
| **最優先** | Firestore セキュリティルールの確認・修正（Firebase Console側） | 小 |
| **高** | 印刷HTML生成のXSSエスケープ統一（全出力に `esc()` 適用） | 小 |
| **高** | JSONインポート時のスキーマバリデーション追加 | 小 |
| **中** | Firebase SDK に SRI ハッシュ追加 | 小 |
| **中** | CSP meta タグ追加 | 小 |
| **低** | PII の暗号化（またはFirestoreルールでの厳格なアクセス制御で代替） | 中 |
